{
  "name": "plain-mihoyo-launcher",
  "scripts": {
    "test": "npm run build && npm start",
    "build": "tsc -p tsconfig/entry.json && webpack",
    "dist": "npm run build && eop build",
    "start": "chcp 65001 && eop start"
  },
  "dependencies": {
    "@types/adm-zip": "^0.5.5",
    "adm-zip": "^0.5.14",
    "electron-ofp": "^1.0.101",
    "save-tool": "^1.0.3",
    "ts-loader": "^9.5.1",
    "type-electron": "^1.0.4",
    "typescript": "^5.5.3",
    "webpack": "^5.92.1",
    "webpack-cli": "^5.1.4"
  }
}
